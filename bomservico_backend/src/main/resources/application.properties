spring.application.name=bomservico

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/bomservico_db
spring.datasource.username=postgres
spring.datasource.password=32653665
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=bomservico-secret-key-change-this-in-production-to-a-more-secure-random-string-min-256-bits
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=15MB
file.upload-dir=uploads/fotos

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:8080,http://localhost:5500,http://localhost:8000,http://127.0.0.1:5500,http://127.0.0.1:8000,http://127.0.0.1:8080,http://127.0.0.1:3000
